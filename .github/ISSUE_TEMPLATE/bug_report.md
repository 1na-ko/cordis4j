---
name: Bug report
about: Report a defect in cordis4j-core or a demo
labels: bug
assignees: ''
---

**Environment**

- Cordis4j version (`mvn dependency:list | grep cordis4j` or the git revision):
- JDK version (`java -version`):
- OS:

**Paper/contract reference**

Which construct does this involve (if applicable)? For example: withdrawal drain
(Algorithm 5 / Theorem 63), reactive activation (Algorithm 3), guard (Section 4.3.2).

**Minimal reproduction**

```java
// A self-contained snippet or test that reproduces the behavior
```

**Expected behavior**

What the paper or docs/design-contract.md (boundary semantics clauses) requires.

**Actual behavior**

What happened instead, including the full stack trace if an exception was thrown.

**Additional context**

Add any other context (configuration, concurrency involved, virtual threads involved).
